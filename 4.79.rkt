#lang sicp
;; REVISIT -- worth Ph.D.?

#|
We'd want to create an environment structure which is extended each time we enter a rule body.
A variable in a rule body would shadow one with the same name in an enclosing frame.
|#
